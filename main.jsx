import React from 'react';
import ReactDOM from 'react-dom/client';
import PreRepeter from './PreRepeter';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PreRepeter />
  </React.StrictMode>
);
